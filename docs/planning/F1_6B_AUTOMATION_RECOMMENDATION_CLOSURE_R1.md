# F1-6B-AUTOMATION-RECOMMENDATION-CLOSURE-R1 — Weekly Inventory + Monthly Order Recommendation

**Outcome: IMPLEMENTED (no HALT).** Baseline HEAD `12ce1ea`. The ambiguous single "Weekly Recommendation" automation
(which ran BOTH INVENTORY + ORDER_PLANNING) is RETIRED and split into two canonical, product-isolated automations with
their own prerequisite Gap gate. The Monthly Order path persists actionable Request Order drafts via the EXISTING
49_→48_ run (built in `12ce1ea`); Weekly Inventory runs the gated canonical INVENTORY runtime. REUSE only — no second
engine/scheduler/persister, no new DB schema. Owners: [`45_`](../../assets/specs/active/apps-script/45_api_v1_automation_schedule.gs),
[`47_`](../../assets/specs/active/apps-script/47_api_v1_recommendation_generation.gs),
[`automation-schedule.js`](../../assets/js/pages/automation-schedule.js).

## §0 Audit — proven current state (before edit)
- **Old ambiguous coupling** = `AUTOMATION_JOBS_` single entry `{ key:'weeklyRecommendation', handler:'runWeeklyRecommendation' }` (45_) → `runWeeklyRecommendation` (47_) which invoked **both** `weeklyRecoStart_` (ORDER_PLANNING persistence) **and** `runRecommendationGeneration('INVENTORY')` (summary). The registry entry + handler are the exact owners.
- **Inventory Gap readiness** = `recGenReadGapJobState_('INVENTORY')`/`recGenGapReadyFromState_` (47_) over the 46_ job state; enforced inside `runRecommendationGeneration` §11 (GAP_JOB_NOT_DONE). **Order Planning Gap readiness** = same for `'ORDER_PLANNING'`, enforced in `runRecommendationGeneration` and again in the 49_ run + each 48_ scope START.
- **Canonical Recommendation runtime** = KMREC via `runRecommendationGeneration` (47_). **Canonical persistence owner** = 24_ `rpoGenerateRecommendationDraftLockedResult_` (KMPW/KMPR) → `request_order_allocation_drafts`. **Canonical resumable job** = 48_ (ORDER_PLANNING). **Scheduled OP persistence driver** = 49_ (`12ce1ea`).
- **INVENTORY has NO resumable, backend-drivable actionable-draft persister**: `shipping_allocation_drafts` are written by **browser-driven per-SKU upserts** (16_ `upsertShippingAllocationDraft`/`_Lines`, driven from `inventory-replenishment.js`); the recommendation workspace (42_) is READ-ONLY. So a scheduled inventory batch persister would be a SECOND engine (forbidden) → Weekly Inventory runs the gap-gated canonical runtime (§E1 "where product semantics require it").
- **Settings owner** = Script Properties `KM_AUTOMATION_SCHEDULE_CONFIG` (45_). **Trigger reconciler** = `automationReconcileTrigger_` (45_, delete-then-create, strict allowlist). **Timezone** = `AUTOMATION_TZ_='Asia/Taipei'`. **MONTHLY/day-of-month did NOT exist** (validation/reconciler were DAILY/WEEKLY only) → extended minimally (no HALT: existing registry + `ClockTriggerBuilder.onMonthDay` cover it).

## §22 Completion report
1. PRE HEAD `12ce1ea`. 2. POST HEAD this commit.
3. **Old Weekly owner** = 45_ `weeklyRecommendation` registry entry → 47_ `runWeeklyRecommendation`. 4. **Old coupling** = that one handler ran BOTH INVENTORY (summary) + ORDER_PLANNING (persistence). 5. **Inventory Gap owner** = `recGenReadGapJobState_('INVENTORY')` (46_ state, gate in `runRecommendationGeneration`). 6. **OP Gap owner** = same for ORDER_PLANNING (+ the 49_/48_ gates). 7. **Canonical runtime** = KMREC via `runRecommendationGeneration`. 8. **Persistence owner** = 24_ locked persister → `request_order_allocation_drafts`. 9. **Resumable job** = 48_. 10. **Settings owner** = Script Properties `KM_AUTOMATION_SCHEDULE_CONFIG` (45_). 11. **Reconciler** = `automationReconcileTrigger_` (45_). 12. **Timezone** = Asia/Taipei.
13. **Weekly Inventory registry key** = `weeklyInventoryRecommendation`. 14. **Weekly trigger handler** = `runWeeklyInventoryRecommendation` (47_). 15. **Weekly default** = WEEKLY, Monday 14:00 Asia/Taipei, disabled (opt-in). 16. **Weekly prerequisite** = Inventory Gap DONE/READY (`runRecommendationGeneration('INVENTORY')` §11 gate → BLOCKED if not). 17. **Monthly registry key** = `monthlyOrderRecommendation`. 18. **Monthly trigger handler** = `runMonthlyOrderRecommendation` (47_). 19. **Monthly default** = MONTHLY, **day 10** 14:00 Asia/Taipei, disabled (opt-in). 20. **Monthly prerequisite** = Order Planning Gap DONE/READY (gated inside the 49_ run + each 48_ START). 21. **Old Weekly migration/retirement** = registry entry REMOVED; `runWeeklyRecommendation` in `AUTOMATION_RETIRED_HANDLERS_` (deletable, never creatable) + swept on every Save & Apply (`automationSweepRetiredTriggers_`); a stored legacy `weeklyRecommendation` config block is reinterpreted as `weeklyInventoryRecommendation` in `automationReadConfig_` (NEVER copied to Monthly); a self-retiring shim `runWeeklyRecommendation` (47_) deletes its own lingering trigger and delegates to INVENTORY only.
22. **Manual AI Plan persistence chain** = request-order page → 48_ start/continue → 24_ locked persister → drafts. 23. **Scheduled persistence chain** = `runMonthlyOrderRecommendation` → 49_ `weeklyRecoStart_` → self-arming continuation → drives 48_ per scope (`SCHEDULED_REFRESH`) → 24_ → drafts. 24. **Exact convergence point** = the 48_ job (`reqDraftJobStart_`/`reqDraftJobContinue_`) → 24_ `rpoGenerateRecommendationDraftLockedResult_`. 25. **Continuation owner** = 49_ `weeklyRecoContinue_`. 26. **Continuation trigger owner** = 49_ `continueWeeklyRecommendationJob` (one-off, reuses 46_ `.after()` + safe delete-by-handler). 27. **Continuation cleanup** = clear-before-arm at-most-one; cleared on DONE/terminal. 28. **planning_cycle authority** = `gapCalcResolveContext_('ORDER_PLANNING')` (43_), resolved in 49_. 29. **Draft identity** = 24_/KMPR `MONTHLY_ORDER::planning_cycle|company|country|marketplace|draft_purpose|sku` (unchanged). 30. **generation_type/provenance** = `SCHEDULED_REFRESH` → existing `generation_type: 'scheduled'` (no new vocabulary). 31. **recommended_qty authority** = verbatim `order_planning_gap` (unchanged). 32. **order_qty protection** = `preserveUserQty` (SCHEDULED_REFRESH holds recommended_qty immutable within a draft_version; a regenerate over user edits needs confirmation). 33. **Active conflict** = 24_/KMPR CREATE/REUSE/BLOCKED_CONFLICT + 48_ single-active + lease (unchanged, not weakened).
34. **Inventory-gap-not-ready** = `runWeeklyInventoryRecommendation` returns `{ ok:false, status:'BLOCKED', reason:'GAP_JOB_NOT_DONE'|... }` — never a success. 35. **Order-gap-not-ready** = `runMonthlyOrderRecommendation` returns `{ status:'BLOCKED', reason:'ORDER_PLANNING_GAP_NOT_READY' }` (49_ SKIPPED). 36. **Stale/mismatched gap** = the canonical gate consumes only the materialized gap authority (48_'s `reqDraftJobBindingChanged_` also fails closed GAP_GENERATION_CHANGED mid-run); no prior-cycle/partial/RUNNING fallback, no hidden recompute. 37. **Blocked notification/status** = the backend returns a truthful BLOCKED result (code + reason) — never reported as success; no persistent Last-Run schema is invented (`AUTOMATION_BLOCKED_NOTIFICATION_PERSISTENCE_GAP` — the persistent-history UI is deferred, but the safety gate is enforced). 38. **Duplicate Weekly proof** = reconciler delete-then-create → exactly one `runWeeklyInventoryRecommendation` trigger; the summary is gap-gated + non-persistent (idempotent). 39. **Duplicate Monthly proof** = exactly one `runMonthlyOrderRecommendation` trigger; the 49_ run single-instances + 48_ single-active (no duplicate draft). 40. **Manual/scheduled collision** = 48_ single-active + lease + 24_ BLOCKED_CONFLICT/user-edit protection (a foreign manual job is deferred, never overwritten). 41. **Timeout/retry** = 49_ durable cursor + recovery trigger + 48_ idempotent REUSE; trigger-arm throw fails closed. 42. **No-browser proof** = the 49_ self-arming continuation chain drives 48_ to completion server-side. 43. **Trigger leak proof** = at-most-one continuation trigger (clear-before-arm), cleared on terminal; retired handler swept; per-job max-one reconcile.

### IMPACT
44. **Files changed** = `45_` (registry split + MONTHLY/dayOfMonth validation·reconciler·view + migration + retirement sweep), `47_` (two product-isolated targets + retiring shim), `49_` (header note only), `automation-schedule.js` (MONTHLY + Day-of-Month UI), `weekly-inventory-monthly-order-automation-f1-6b-r1.test.js` (NEW), `weekly-recommendation-scheduler-f1-6a-r1.test.js` (rewritten → split-migration guard), `automation-schedule-admin-r1.test.js` (updated), `weekly-recommendation-persistence-f1-6b-r1.test.js` (2 assertions repointed), this doc.
45. **Tests added/changed** = 1 new (33 assertions) + 3 updated. 46. **Focused results** = split 33/33 · migration 12/12 · admin 57/57 · persistence 45/45 · document-UI 22/22 · recommendation-generation 47/47 · 48_ job 52/52. 47. **Scheduler regression** = admin 57/57 (unchanged mechanics). 48. **Gap regression** = gap-materialization suites unaffected. 49. **Recommendation regression** = 47/47. 50. **Request Order regression** = 48_ 52/52. 51. **PO/Shipment regression** = unaffected. 52. **Export regression** = R2B/R3A/R3B/R3C + document-UI unaffected. 53. **Production-safety** = unaffected (no migration twins). 54. **Full regression** = **212 files; only the 4 known baseline failures** (`gap-job-done-notice-f1-small-r1`, `order-planning-monthly-projection-consumer-f1-4b-fm3d`, `replen-header-toggle`, `supply-planning-route-inventory`) — none new.
55. **Apps Script sync** = `45_` + `47_` (`49_` changed only a header comment — sync optional/harmless). Router UNCHANGED. 56. **Frontend deploy** = `assets/js/pages/automation-schedule.js`. 57. **Bundle rebuild** = NO. 58. **DB/schema impact** = NONE (config stays in the Script Property; drafts persist into existing columns). 59. **API impact** = `automationSchedule.update` config accepts `frequency:'MONTHLY'` + `dayOfMonth` (backward compatible; the adapter forwards config generically). 60. **Formula impact** = NONE. 61. **Forecast/Gap impact** = NONE. 62. **Request Order impact** = NONE (same drafts the manual AI Plan produces). 63. **PO impact** = NONE. 64. **Shipment impact** = NONE. 65. **Export impact** = NONE.
66. **USER live verification** =
   - Sync `45_` + `47_`; ensure ScriptApp trigger auth is granted (same scope 46_ uses).
   - Administration → Automation Schedule now shows **Weekly Inventory Recommendation** (Weekly) and **Monthly Order Recommendation** (Monthly, Day of Month = 10) as independent cards. Enable + Save & Apply each → exactly one trigger each; the pre-migration `runWeeklyRecommendation` trigger is swept (verify only `runWeeklyInventoryRecommendation` + `runMonthlyOrderRecommendation` exist — no `runWeeklyRecommendation`).
   - Before the Order Planning gap is DONE, `runMonthlyOrderRecommendation` returns BLOCKED (`ORDER_PLANNING_GAP_NOT_READY`); after DONE it persists drafts (visible via `requestOrderDraft.getActive`, `generation_type='scheduled'`). `runWeeklyInventoryRecommendation` returns BLOCKED until the Inventory gap is DONE, then runs the canonical INVENTORY runtime.
   - Editing the Monthly Day of Month / time and re-saving replaces the trigger (still max one).
67. **Remaining Phase-1 E2E blockers** = INVENTORY scheduled actionable-draft PERSISTENCE is intentionally NOT built (no resumable backend-drivable authority; would be a second engine) — Weekly Inventory is the gap-gated canonical runtime; a resumable inventory draft job is a separate authorized design. `AUTOMATION_BLOCKED_NOTIFICATION_PERSISTENCE_GAP` (persistent Last-Run history UI) deferred — the safety gate is enforced regardless. No blocker remains on the Monthly Order → actionable-draft path. 68. **Commit hash** = chat. 69. **Next authorized slice** = (a) a resumable INVENTORY draft persistence job (if scheduled inventory drafts are wanted); (b) a persistent Last-Run / blocked-status Administration surface (needs a small authorized schema); then F1-PHASE1-E2E-FINAL.

## §23 FINAL GATE — all ✓
Weekly Inventory ≠ Monthly Order ✓ · Weekly = INVENTORY only ✓ · Monthly = ORDER_PLANNING only ✓ · Monthly default day
10 ✓ · both independently configurable ✓ · max one trigger each ✓ · old ambiguous Weekly trigger retired (swept) ✓ ·
Inventory Gap prerequisite enforced ✓ · Order Planning Gap prerequisite enforced ✓ · missing/stale Gap → BLOCKED ✓ ·
blocked ≠ success ✓ · no stale fallback ✓ · no hidden Gap recompute ✓ · scheduled generation persists actionable
canonical drafts (Monthly) ✓ · same canonical runtime ✓ · same canonical persistence authority ✓ · manual + scheduled
converge ✓ · no browser needed for scheduled completion ✓ · resumable / retry safe ✓ · no continuation trigger leak ✓ ·
user edits protected ✓ · no duplicate active draft ✓ · no second Recommendation/Gap/scheduler/persister ✓ · Recommendation
/ Forecast / Gap formulas unchanged ✓ · RO/PO/Shipment/Export unchanged ✓ · focused tests green ✓ · no new
full-regression failures ✓.

**STOP after F1-6B-AUTOMATION-RECOMMENDATION-CLOSURE-R1.** Do NOT begin F1-PHASE1-E2E-FINAL, new Export document
families, Customs authority work, or unrelated UI redesign.
